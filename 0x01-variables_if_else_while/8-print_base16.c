#include <stdio.h>
/**
 * main - print all numbers of base 16 in lowercase.
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int i;

	for (i = 48; i < 58: i++)
	{
		putchar (i);
	}
	for (i = 61; i < 67; i++)
	{
		putchar (i);
	}
	putchar ('\n');
	return (0);
}
